export const NotificationType = {
    DIRECT_SHARE: 'direct_share',
    REACTION: 'reaction',   // Must match DB exactly
    COMMENT: 'comment',     // Must match DB exactly
    PLAY_EVENT: 'play_event' // Must match DB exactly
};